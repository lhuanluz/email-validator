[package]
name = "email-validator"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1.5"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
 
